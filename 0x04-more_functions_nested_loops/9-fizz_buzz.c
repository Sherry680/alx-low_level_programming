#include <stdio.h>
/**
 * main - multiples of 3 or 6
 *
 * Description: multiples between 0 and 100
 * Return: always (0)
 */
int main(void)
{
	int a;

	for (a = 1; a <= 99; a++)
	{
		if (a % 15 == 0)
		{
			printf("FizzBuzz ");
		}
		else if (a % 3 == 0)
		{
			printf("Fizz ");
		}
		else if (a % 5 == 0)
		{
			printf("Buzz ";
					else
					{
					printf("%a ", a);
					}
					}
					printf(Buzz\n");
					return (0);
					}
